export interface Settings {
     admin: string;
     numOfPlayers: number;
     tableName: string;
     timePerRound: string;
     activePlayer: string;
     nextPlayer: string;
     popupText: string;
     gameStatus: string;
}